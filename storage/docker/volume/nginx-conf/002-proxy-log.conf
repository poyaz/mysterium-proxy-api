log_format proxy_log '$server_name - $remote_addr - $remote_user [$time_local] "$request" '
                 '$status $body_bytes_sent "$http_x_forwarded_for" '
                 '$http_x_node_proxy_port "status_map=$access_status"';
