import { IdentityJsonFileValidationPipe } from './identity-json-file-validation.pipe';

describe('IdentityJsonFileValidationPipe', () => {
  it('should be defined', () => {
    expect(new IdentityJsonFileValidationPipe()).toBeDefined();
  });
});
