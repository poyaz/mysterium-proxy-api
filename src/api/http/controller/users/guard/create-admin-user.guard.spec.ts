import { CreateAdminUserGuard } from './create-admin-user.guard';

describe('CreateAdminUserGuard', () => {
  it('should be defined', () => {
    expect(new CreateAdminUserGuard()).toBeDefined();
  });
});
