import { RemovePasswordFieldOfUserInterceptor } from './remove-password-field-of-user.interceptor';

describe('RemovePasswordFieldOfUserInterceptor', () => {
  it('should be defined', () => {
    expect(new RemovePasswordFieldOfUserInterceptor()).toBeDefined();
  });
});
