import { ChangeOwnPasswordInterceptor } from './change-own-password.interceptor';

describe('ChangeOwnPasswordInterceptor', () => {
  it('should be defined', () => {
    expect(new ChangeOwnPasswordInterceptor()).toBeDefined();
  });
});
