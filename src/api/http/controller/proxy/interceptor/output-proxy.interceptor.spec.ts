import { OutputProxyInterceptor } from './output-proxy.interceptor';

describe('OutputProxyInterceptor', () => {
  it('should be defined', () => {
    expect(new OutputProxyInterceptor()).toBeDefined();
  });
});
